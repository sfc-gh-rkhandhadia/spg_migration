[project]
name = "mssql-ddl-realization"
version = "0.1.0"
description = "MSSQL DDL Realization: parse, deploy, seed, and validate MSSQL schemas in Docker"
requires-python = ">=3.11"
dependencies = [
    "pymssql>=2.3.0",
    "faker>=24.0.0",
    "psycopg2-binary>=2.9.12",
]
